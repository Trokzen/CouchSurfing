```
# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Databases
*.sqlite
*.db

# Environment
.env
.env.local
*.env.*

# Logs
*.log

# Temporary files
*.tmp
*.temp
*~
```

Wait, let me reconsider. The only artifact in the changes is a compiled Python file (`__pycache__/listing.cpython-312.pyc`), but there are also SQL migration files and database setup files which suggest this might be a Python web application with database migrations. However, following rule #6, since there are source/config files mixed with artifacts (the SQL files are config/migration files), I should include the relevant ignores for the detected artifacts:

```
# Python
__pycache__/
*.pyc
```